syntax = "proto3";

package collab;

// A snapshot of a collaborative document.
message CollabSnapshot {
  // yrs snapshot encoded using EncoderV1.
  bytes data = 1;
  // The creation timestamp of the snapshot.
  int64 created_at = 2;
}
